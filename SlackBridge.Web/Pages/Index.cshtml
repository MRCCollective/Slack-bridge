@page
@model IndexModel
@{
    ViewData["Title"] = "Slack Bridge";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h1 class="mb-2">Slack Bridge</h1>
        <p class="text-muted mb-0">Send events to one API. Render templates. Deliver Slack messages.</p>
    </div>
    <a class="btn btn-primary" asp-page="/Admin/EventDefinitions/Index">Send event</a>
</div>

<pre class="bg-light border rounded p-3"><code>POST /api/events
x-api-key: sb_...

{
  "key": "build-failed",
  "data": {
    "repository": "checkout",
    "branch": "main",
    "url": "https://example.test/builds/42"
  }
}</code></pre>
