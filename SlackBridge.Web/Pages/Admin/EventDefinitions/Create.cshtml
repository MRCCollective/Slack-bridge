@page
@model SlackBridge.Web.Pages.Admin.EventDefinitions.CreateModel
@{
    ViewData["Title"] = "New event";
}

<h1>New event</h1>
@if (!string.IsNullOrWhiteSpace(Model.StatusMessage))
{
    <div class="alert @(Model.StatusSucceeded ? "alert-success" : "alert-danger")" role="alert">
        @Model.StatusMessage
    </div>
}
<form method="post">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <div class="row">
        <div class="col-lg-6 mb-3">
            <label asp-for="EventDefinition.ProjectId" class="form-label">Project</label>
            <select asp-for="EventDefinition.ProjectId" asp-items="Model.ProjectOptions" class="form-select"></select>
        </div>
        <div class="col-lg-6 mb-3">
            <label asp-for="EventDefinition.Key" class="form-label"></label>
            <input asp-for="EventDefinition.Key" class="form-control" placeholder="build-failed" />
            <span asp-validation-for="EventDefinition.Key" class="text-danger"></span>
        </div>
    </div>
    <div class="mb-3">
        <label asp-for="EventDefinition.SlackWebhookUrl" class="form-label"></label>
        <input asp-for="EventDefinition.SlackWebhookUrl" class="form-control" />
        <span asp-validation-for="EventDefinition.SlackWebhookUrl" class="text-danger"></span>
    </div>
    <div class="mb-3">
        <label asp-for="EventDefinition.Template" class="form-label"></label>
        <textarea asp-for="EventDefinition.Template" class="form-control font-monospace" rows="10"></textarea>
        <span asp-validation-for="EventDefinition.Template" class="text-danger"></span>
    </div>
    <div class="form-check mb-3">
        <input asp-for="EventDefinition.IsActive" class="form-check-input" />
        <label asp-for="EventDefinition.IsActive" class="form-check-label"></label>
    </div>
    <button class="btn btn-primary" type="submit">Create</button>
    <button class="btn btn-outline-secondary" type="submit" asp-page-handler="Test">Send test</button>
    <a class="btn btn-link" asp-page="Index">Cancel</a>
</form>

@section Scripts { <partial name="_ValidationScriptsPartial" /> }
